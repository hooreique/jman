AccessService.isAdmin(" admin ") unexpectedly returns false in the application.
Find the actual cause and fix the application code. Keep non-admin users rejected.
Explain which implementation of TextUtil.normalize the application actually calls,
including its dependency version. Do not change the public library behavior.
