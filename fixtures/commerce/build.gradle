subprojects {
    apply plugin: 'java'
    java { sourceCompatibility = JavaVersion.VERSION_17; targetCompatibility = JavaVersion.VERSION_17 }
    repositories { maven { url = uri(rootProject.file('../maven-repository')) } }
}
project(':app') {
    dependencies { implementation 'com.acme:shared-text:2.4.1' }
}
